apiVersion: v1
kind: ConfigMap
metadata:
  name: nexa-scheduler-policy
  namespace: nexa-system
  labels:
    app.kubernetes.io/name: nexa-scheduler
data:
  policy.json: |
    {
      "regionPolicy": {
        "enabled": true,
        "defaultRegion": "",
        "defaultZone": ""
      },
      "privacyPolicy": {
        "enabled": true,
        "defaultPrivacy": "standard",
        "strictOrgIsolation": false
      }
    }
