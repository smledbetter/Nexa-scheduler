apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexa-scheduler.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nexa-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "nexa-scheduler.selectorLabels" . | nindent 4 }}
