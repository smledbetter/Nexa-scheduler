run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - typecheck
    - goimports

linters-settings:
  goimports:
    local-prefixes: github.com/nexascheduler/nexa
